using System.Collections.Generic;
using UnityEngine;

public class PolarBearGameManager : MonoBehaviour
{
    //[SerializeField] private GameObject fishingHoleList;
    //private List<FishingHoleLogic> fishingHoles = new List<FishingHoleLogic>();
    //private List<FishingHoleLogic> activatedfishingHoles = new List<FishingHoleLogic>();
    //private float timer = 0;

    void Start()
    {
        //foreach (Transform child in fishingHoleList.transform)
        //{
        //    fishingHoles.Add(child.gameObject.GetComponent<FishingHoleLogic>);
        //}
    }

    // Update is called once per frame
    void Update()
    {
        //    timer += Time.deltaTime;
        //    if (timer >= 1)
        //    {
        //        foreach (GameObject hole in activatedfishingHoles)
        //        {
        //            hole.deac
        //        }
        //        foreach(GameObject hole in fishingHoles)
        //        {
        //            Random.Range(0,2)
        //        }
        //    }

    }
}
